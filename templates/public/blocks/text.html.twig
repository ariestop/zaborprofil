<section class="prose max-w-none" data-block-type="text">
    <h2>{{ block.content.title|default(block.name) }}</h2>
    {% if block.content.text is defined %}
        <div>{{ block.content.text|nl2br }}</div>
    {% endif %}
</section>
