# Module

Каталог модулей модульного монолита.

Каждый модуль развивается по слоям:

- `Domain`
- `Application`
- `Infrastructure`
- `UI`

На первом этапе здесь создается только минимальная структура и код, необходимый для запуска фундамента: `Admin`, `Auth`, `User`.
